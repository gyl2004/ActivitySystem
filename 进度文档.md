# 公益活动管理系统 - 进度文档

## 项目初始化

### 1. 2026-03-27: 项目基础结构搭建
- **描述**: 初始化 Spring Boot 3.1.5 项目结构，配置 Maven 依赖。
- **关键技术**: 
  - Java 17
  - Spring Boot 3.1.5
  - MyBatis Plus 3.5.3
  - MySQL 8.0
  - Redis, JWT, Spring Security
  - Springdoc OpenAPI (Swagger)
- **解决内容**: 完成了 `pom.xml` 编写及启动类 `ActivitySystemApplication` 的创建。

### 2. 2026-03-27: 核心业务与重点功能开发
- **描述**: 完成了第一阶段（活动、报名、签到、统计）和第二阶段（评价、推荐、地图、消息）的核心代码实现。
- **关键技术**: 
  - **活动评价**: 集成 AI 情感分析与敏感词过滤，支持二级回复、点赞互动。
  - **活动推荐**: 基于 AOP 的用户行为追踪，实现推荐引擎骨架（UserCF/ItemCF 扩展点）。
  - **地图模块**: 实现基于 Haversine 公式的附近活动搜索、地理编码与围栏校验。
  - **消息通知**: 异步站内信与邮件通知机制。
- **解决内容**: 
  - 实现了 `ReviewService`, `RecommendationService`, `MapService`, `MessageService` 及其对应的控制器。
  - 引入了 `ActivityBehaviorAspect` 用于无侵入式采集用户行为数据。
  - 提供了统一的 AI 接口层 `AIService`。

### 3. 2026-03-27: 前端工程化与 UI 交付
- **描述**: 完成了前端项目的初始化与全量核心页面的 UI 逻辑。
- **关键技术**: 
  - **Vue 3 + Vite**: 极速的热重载开发体验。
  - **Tailwind CSS**: 响应式工具链，定制了阳光绿 (Primary-500) 主色调。
  - **响应式设计**: 完美适配移动端与桌面端。
  - **路由架构**: 实现了嵌套路由与后台管理布局。
- **解决内容**: 
  - 修复了 `AdminLayout` 引用缺失导致的 Vite 编译错误。
  - 交付了首页、活动广场、详情页、登录页、个人中心及管理后台仪表盘。

### 4. 2026-03-27: 前后端全量对接与认证增强
- **描述**: 移除了第三方登录，实现了原生注册功能，并完成了所有业务模块的前后端 API 对接。
- **关键技术**: 
  - **Axios 拦截器**: 统一处理 JWT Token 注入与 401/500 异常捕获。
  - **CORS 跨域**: 后端配置跨域资源共享，支持前端本地开发调试。
  - **数据闭环**: 首页统计、活动列表分页、详情页报名/评价、个人中心数据均已真实接入后端。
- **解决内容**: 
  - 新增了 `Register.vue` 页面与后端 `/api/auth/register` 接口。
  - 在 `AuthController` 中新增了 `me` 接口用于同步用户信息。
  - 实现了活动报名、评价提交的真实业务流。

### 5. 2026-03-27: 移除社交登录与分类接口完善
- **描述**: 彻底移除 Google 和 GitHub 登录方式，完善原生注册流程，并新增活动分类 API 替代前端 Mock 数据。
- **关键技术**: 
  - **分类 API**: 新增 `ActivityCategoryController` 与服务层，实现动态分类管理。
  - **管理端增强**: 更新 `AdminLayout.vue`，实现管理员信息动态获取与安全退出逻辑。
  - **接口闭环**: 修复了 `ActivityList.vue` 中硬编码的分类数据，实现全站真实数据驱动。
- **解决内容**: 
  - 后端：新增了 `ActivityCategoryService` 及其实现类。
  - 前端：更新了 `ActivityList.vue` 和 `AdminLayout.vue` 的 API 调用逻辑。

### 6. 2026-03-27: 管理后台功能增强与种子数据填充
- **描述**: 添加了系统管理员账号，完善了管理后台的子页面（活动管理、报名审核、评价管理），并填充了初始业务数据。
- **关键技术**: 
  - **RBAC 种子数据**: 提供了 `seed_data.sql`，包含管理员账号（密码 `admin123`）、志愿者账号及完整的权限分配逻辑。
  - **管理端子页面**: 交付了 `ActivityManage.vue` (CRUD)、`RegistrationAudit.vue` (审核流)、`ReviewManage.vue` (情感分析展示)。
  - **路由鉴权**: 完善了前端路由守卫，支持 `requiresAuth` 和 `requiresAdmin` 校验。
- **解决内容**: 
  - 提供了 `db/seed_data.sql` 文件，便于一键初始化系统环境。
  - 完善了 `AdminLayout` 导航及子路由配置。

### 7. 2026-03-27: 管理员角色识别与 UI 自动切换
- **描述**: 完善了角色识别机制，确保管理员登录后自动进入管理后台，并在前台导航栏显示管理入口。
- **关键技术**: 
  - **后端增强**: `AuthController.login` 和 `me` 接口现在会返回用户的角色信息。
  - **登录重定向**: 管理员登录后由 `/` 自动重定向至 `/admin/dashboard`。
  - **UI 动态展示**: `MainLayout` 和 `Profile` 页面会根据用户角色动态显示“管理后台”入口。
- **解决内容**: 
  - 修复了管理员登录后界面与普通用户无差异的问题。
  - 提供了便捷的后台入口切换功能。

### 8. 2026-03-27: 管理后台全量功能对接与系统设置
- **描述**: 完成了管理后台所有子页面的功能闭环，包括活动发布流、报名审核流、评价审核流及系统参数配置。
- **关键技术**: 
  - **活动管理增强**: `ActivityManage.vue` 现支持创建、编辑、发布、取消等完整生命周期管理。
  - **全量审核接口**: 后端新增了报名记录分页查询和评价记录分页查询接口，支持管理员全局视角审核。
  - **系统设置模块**: 交付了 `SysConfig` 后端组件及 `SystemSettings.vue` 页面，支持动态调整应用名称、自动发布开关等。
  - **UI/UX 优化**: 统一了管理端弹窗样式，优化了侧边栏激活状态及用户信息获取逻辑。
- **解决内容**: 
  - 解决了管理后台部分页面数据 Mock 或接口不匹配的问题。
  - 补全了缺失的“系统设置”模块，实现了系统参数的持久化管理。

### 9. 2026-03-30: 简历导向项目深度优化 (Resume Ready)
- **描述**: 针对求职简历需求，对项目进行了深度技术优化，增强了系统的并发处理能力与算法逻辑。
- **关键技术**: 
  - **多级缓存架构**: 集成 Redis 实现了推荐结果、热门排行、详情数据的高效缓存，大幅提升系统吞吐量。
  - **实时热度算法**: 利用 Redis ZSet 实现基于用户行为（浏览、报名、签到、评价）的实时权重累加，替代传统数据库统计，实现秒级热点响应。
  - **推荐引擎升级**: 实现了多级兜底推荐策略（个性化召回 -> 启发式分类偏好 -> 热门排行），增强了系统在冷启动情况下的用户体验。
  - **高可用设计**: 引入 AOP 异步记录行为日志，解耦核心业务逻辑与数据采集，提升接口响应性能。
- **解决内容**: 
  - 重构了 `RecommendationServiceImpl` 和 `UserBehaviorServiceImpl`。
  - 优化了全量 Controller 的 API 文档注解，使其更符合生产环境交付标准。
  - 完善了 DTO 数据校验，确保护航后端业务安全性。

### 10. 2026-04-03: 搜索引擎集成、异步通知与业务闭环完善
- **描述**: 引入了 Elasticsearch 和 RabbitMQ，并完善了活动管理与报名的多个高级功能。
- **关键技术**: 
  - **Elasticsearch**: 实现了活动标题、摘要、内容的全文检索，并支持地理位置索引（GeoPoint）。
  - **RabbitMQ**: 建立了异步通知机制，将报名提交、审核结果、退款通知等业务解耦。
  - **业务增强**: 实现了活动一键复制、Excel 批量导出、报名自动候补位逻辑及批量审核功能。
- **解决内容**: 
  - 集成了 `spring-boot-starter-data-elasticsearch` 和 `spring-boot-starter-amqp`。
  - 在 `ActivityServiceImpl` 中实现了 ES 同步逻辑与 `search` 接口。
  - 在 `RegistrationServiceImpl` 中实现了基于 MQ 的 `sendNotify` 和候补自动补位算法。
  - 提供了 `copyActivity`, `exportActivities`, `batchAudit`, `refund` 等多个新 API。

### 11. 2026-04-03: 报名导出、签到增强与地图服务完善
- **描述**: 完善了报名数据管理、签到交互安全及基于 ES 的地理位置服务。
- **关键技术**: 
  - **Excel 导出**: 基于 Hutool 实现报名记录的多条件筛选导出。
  - **动态二维码**: 实现了基于 Redis Token 的活动签到二维码生成（Base64），增强了签到安全性。
  - **ES Geo-Search**: 实现了基于地理位置的附近活动搜索，支持按距离排序。
  - **WebSocket 广播**: 实现了实时签到数据推送，支持管理端或数据大屏实时监控。
- **解决内容**: 
  - 完善了 `RegistrationController` 和 `CheckinController` 的 API。
  - 在 `ActivityRegistrationMapper` 中实现了多表关联查询（VO）。
  - 在 `CheckinServiceImpl` 中集成了 Redis 校验签到 Token 的逻辑。
 
### 12. 2026-04-03: 修复新增接口 403 权限问题
- **描述**: 新增导出/签到二维码等接口上线后，管理员访问返回 403。
- **原因**: Controller 使用了新的 `@PreAuthorize` 权限键，但种子数据未初始化并绑定到管理员角色，导致已登录也被拒绝访问。
- **解决内容**:
  - 在 `seed_data.sql` 补齐 `activity:export`、`registration:export`、`checkin:audit` 等权限，并关联管理员角色。

### 13. 2026-04-03: 修复 Excel 导出 500（缺少 POI 依赖）
- **描述**: 调用活动/报名导出接口返回 500，日志提示缺少 `poi-ooxml`。
- **原因**: Hutool 的 `ExcelUtil.getWriter` 依赖 Apache POI 的 OOXML 实现包，项目未引入对应依赖。
- **解决内容**:
  - 在 `pom.xml` 增加 `org.apache.poi:poi-ooxml` 依赖，恢复 Excel 导出能力。

---

## 问题与解决记录

| 日期 | 问题描述 | 解决方案 | 状态 |
| :--- | :--- | :--- | :--- |
| 2026-03-27 | 初始项目骨架搭建 | 完成基础 Maven 依赖配置与启动类创建 | 已完成 |
| 2026-03-27 | 核心业务模块第一阶段开发 | 完成活动管理、报名管理、签到与基础统计逻辑 | 已完成 |
| 2026-03-27 | 重点功能模块第二阶段开发 | 完成评价、推荐骨架、地图与消息通知逻辑 | 已完成 |
| 2026-03-27 | 前端 AdminLayout 引用错误 | 补全了 `AdminLayout.vue` 和 `Dashboard.vue` 页面文件 | 已完成 |
| 2026-03-30 | 推荐系统冷启动与性能瓶颈 | 引入多级缓存与 Redis ZSet 实时热度权重计算 | 已完成 |
| 2026-03-27 | 前后端联调与认证增强 | 移除社交登录，新增注册功能，完成 Axios 拦截器与全量 API 对接 | 已完成 |
| 2026-03-27 | 社交登录移除与分类 API 完善 | 移除 Google/GitHub 登录，新增分类 API 并更新管理端 UI 逻辑 | 已完成 |
| 2026-03-27 | 管理后台功能增强与种子数据填充 | 添加管理员账号及种子数据，交付活动/报名/评价管理子页面 | 已完成 |
| 2026-03-27 | 修复分类实体状态字段缺失错误 | 在 ActivityCategory 实体及 SQL 中补全 status 字段，修复编译错误 | 已完成 |
| 2026-03-27 | 管理员界面无差异问题 | 后端返回角色信息，前端实现动态路由跳转与入口展示 | 已完成 |
| 2026-03-27 | 管理后台功能闭环对接 | 补全管理端专用分页接口，完善活动生命周期管理及系统设置模块 | 已完成 |
| 2026-03-27 | 修复 LocalDateTime 反序列化报错 | 添加 Jackson 全局配置，支持 yyyy-MM-dd HH:mm:ss 格式的时间解析 | 已完成 |
| 2026-03-27 | 报名与评价管理功能增强 | 实现实时状态统计、关联用户信息展示及状态筛选功能 | 已完成 |
| 2026-03-27 | 修复 Redis 缓存序列化 LocalDateTime 报错 | RedisTemplate/CacheManager 使用注入的 ObjectMapper 构建 JSON 序列化器，支持 LocalDateTime 写入缓存 | 已完成 |
| 2026-03-27 | 修复 Redis 缓存反序列化类型丢失 | Redis 使用独立 ObjectMapper 开启类型信息，避免 LinkedHashMap 强转实体失败 | 已完成 |
| 2026-03-27 | 修复 Redis ObjectMapper 循环依赖启动失败 | Redis ObjectMapper 改为基于 Jackson2ObjectMapperBuilder 构建，避免与默认 ObjectMapper 循环注入 | 已完成 |
| 2026-03-27 | 修复 Redis 多态白名单拦截 BigDecimal | Redis 多态类型校验放行 java.math 包，避免 BigDecimal 反序列化被拒绝 | 已完成 |
| 2026-03-27 | 修复登录接口缺少 @class 解析失败 | Redis 的 ObjectMapper 不再注册为全局 Bean，避免影响 HTTP JSON 反序列化 | 已完成 |
| 2026-03-27 | 普通志愿者登录后提示“无访问权限” | 移除首页总体统计接口 (`/api/statistics/overall`) 的管理员权限校验 | 已完成 |
| 2026-03-27 | 活动详情页报名状态未能动态显示 | 后端新增获取用户报名状态接口，前端根据状态动态渲染按钮（待审核/已通过/已拒绝）并防止重复报名 | 已完成 |
| 2026-03-27 | 积分与志愿时长未发放及个人中心未展示 | 用户实体及数据库增加积分与时长字段，在签到时根据活动起止时间计算并增加，个人中心绑定真实数据展示 | 已完成 |
| 2026-03-27 | 报名通过后无签到入口 | 后端新增签到状态查询接口，前端在报名通过后显示“立即签到”或“已签到”按钮，点击后调用签到接口发放奖励 | 已完成 |
| 2026-03-27 | 报名状态显示不准确及无法重新报名 | 后端 checkStatus 改为按时间降序取最新记录，并允许已取消报名者重新申请；前端区分显示待审核、已驳回、已取消状态 | 已完成 |
| 2026-03-27 | 活动详情页增加奖励展示与评价增强 | 活动表新增积分与时长字段，详情页展示奖励卡片；评价功能支持展示情感分析、多级标签及用户自定义标签提交 | 已完成 |
| 2026-03-27 | 超级管理员权限不足问题 | 优化 `LoginUser` 的 `getAuthorities`，过滤空权限并正确映射 `SimpleGrantedAuthority`，修复因空字符串导致的 Spring Security 鉴权失败 | 已完成 |
| 2026-03-27 | 发布评价时报 JSON 序列化失败 | 修复前端 `ActivityDetail.vue` 提交评价时，将 `tags` 字段的传参从 `JSON.stringify(reviewTags.value)` 改为 `reviewTags.value` 原生数组，适配后端 DTO 中的 `List<String>` | 已完成 |
| 2026-03-27 | 个人中心显示详细活动奖励 | 在 `activity_registration` 表中增加收益字段，更新签到逻辑自动记录单次收益，前端 `Profile.vue` 显示活动标题、封面图及具体获得的积分与时长 | 已完成 |
| 2026-03-27 | 活动详情页招募人数更新延迟 | 重写 `ActivityServiceImpl.updateById` 并增加 `@CacheEvict` 注解，解决报名审核通过后详情页因 Redis 缓存导致的招募人数显示不准确问题 | 已完成 |
| 2026-03-27 | 评价列表用户头像昵称不一致 | 优化后端 `findPageByActivityId` 接口返回 `ReviewVO` 并关联用户信息，前端更新评价列表展示逻辑，优先显示用户真实头像和昵称 | 已完成 |
| 2026-03-27 | 增加活动“退回草稿”功能 | 后端新增 `revertToDraft` 接口及 Service 逻辑，允许管理员将已发布或已取消的活动退回草稿状态重新编辑；前端管理页面新增相应操作按钮 | 已完成 |
| 2026-03-27 | 实现全站智能推荐系统展示 | 后端新增 `RecommendationController`，前端首页增加“为您推荐”板块（个性化），活动详情页增加“相似活动”推荐，并增强了全方位的用户行为权重追踪 | 已完成 |
| 2026-03-27 | 推荐系统报错 BadSqlGrammarException | 修复 `ActivityRecommendation` 实体中 `rank` 字段与 MySQL 8.0 关键字冲突的问题，通过 `@TableField("`rank`")` 增加转义 | 已完成 |
| 2026-03-27 | 实现新活动发布全站实时弹窗提醒 | 引入 WebSocket 技术栈，实现后端发布活动时向所有在线用户广播消息；前端通过 `ElNotification` 实现全局实时弹窗提醒 | 已完成 |
| 2026-03-27 | 新活动弹窗可点击跳转且详情页滚动异常 | WebSocket 推送改为携带 activityId 的 JSON，前端通知支持点击跳转活动详情；增加路由 scrollBehavior 默认回到顶部并修复详情页切换活动时自动滚动到底部 | 已完成 |
| 2026-03-27 | activity 缓存写入 null 报错 | `ActivityServiceImpl.getById` 的缓存增加 `unless = "#result == null"`，避免 RedisCache 不允许 null 时在报名审核/评价管理场景抛异常 | 已完成 |
| 2026-03-27 | 评价无需审核且支持管理员撤回 | 评价提交默认 `status=1` 立即展示；用户端移除评价标签展示/录入；管理端新增撤回接口 `/api/reviews/{id}/revoke` 用于隐藏不当评论 | 已完成 |
| 2026-03-27 | 管理端撤回评价 403 | 前端请求拦截器在缺少 tokenPrefix 时默认使用 `Bearer` 发送 Authorization，且撤回接口 PUT 改为携带空 body，避免部分环境下鉴权头未带导致 403 | 已完成 |
| 2026-03-27 | 管理端撤回评价仍 403（后端未更新） | 当前运行后端未暴露 `/api/reviews/{id}/revoke`，请求落到 `/error` 后被安全拦截；前端改为复用 `/api/reviews/{id}/audit`（status=2）实现撤回，避免依赖新接口 | 已完成 |
| 2026-03-27 | 评价支持多次提交、回复与恢复 | 移除“同一用户同一活动只能评价一次”的限制；活动详情页支持展开回复与发表评论回复；管理端支持撤回后恢复（复用审核接口 status=1/2） | 已完成 |
| 2026-03-27 | 活动详情页/首页 GET 请求 403 | 更新 `SecurityConfig.java` 允许公开访问活动、分类、评价统计、推荐及总体统计等 GET 接口 | 已完成 |
| 2026-03-27 | 活动默认图片与上传功能增强 | 实现基于分类的自动默认图片填充，新增管理端图片上传（模拟）与实时预览功能，优化列表页默认图展示逻辑 | 已完成 |
| 2026-03-27 | 全站客户端图片同步更新 | 更新首页、活动广场、活动详情及相似推荐页面的图片展示逻辑，确保全站活动图片与分类默认图保持一致 | 已完成 |
| 2026-03-27 | 页脚内容简化 | 移除全站页脚中的“快速链接”与“联系方式”板块，优化底部展示结构 | 已完成 |
| 2026-03-27 | 业务数据扩充 | 在 `activity` 表中新增 5 条覆盖全部分类的真实活动数据，并同步更新至 `seed_data.sql` | 已完成 |
| 2026-04-03 | 搜索引擎、消息队列与业务增强 | 集成 ES 和 RabbitMQ，并实现了活动复制、导出、候补自动补位及批量审核功能 | 已完成 |
| 2026-04-03 | 报名导出与签到安全增强 | 实现报名 Excel 导出、Redis 签到 Token 验证及 ES 附近活动搜索 | 已完成 |
| 2026-04-03 | 修复 Excel 导出时间乱码及冗余字段问题 | 将导出对象转为 Map 并使用 DateTimeFormatter 格式化时间，配合 setOnlyAlias 确保只导出需要的字段，解决 Excel 导出时间显示不正确和字段过多的问题 | 已完成 |
| 2026-04-03 | 修复报名数据导出时报 500 错误的问题 | 移除 `ActivityRegistrationMapper.xml` 中多余且不存在的 `r.deleted = 0` SQL 查询条件，解决调用导出与分页接口时报 `BadSqlGrammarException` 导致服务器 500 错误的问题 | 已完成 |
| 2026-04-03 | 修复发布活动时报 Connection refused 的问题 | `ActivityServiceImpl` 中的 ES 同步和 Redis 缓存清除操作在服务不可用时会抛出 `DataAccessResourceFailureException` 导致事务回滚。通过为 ES 同步增加 `try-catch` 降级，并为 Redis 缓存配置自定义的 `CacheErrorHandler`，实现了核心业务与外部依赖的解耦，确保服务高可用。 | 已完成 |
| 2026-04-03 | 优化签到机制（签到码替代二维码） | 后端移除基于 Base64 生成签到二维码的逻辑，改为生成 6 位随机签到码（存入 Redis），并支持管理员在生成签到码时配置其有效限时时长（默认 5 分钟）。前端 `ActivityManage.vue` 对应修改了获取和展示弹窗的逻辑，以超大加粗字体呈现 6 位签到码；前端 `ActivityDetail.vue` 对应修改了提交签到请求的交互文案与 6 位数字正则表达式校验，极大简化了移动端与网页端的交互操作体验。 | 已完成 |
| 2026-04-03 | 修复前台 WebSocket 弹窗消息展示为原始 JSON 的问题 | 修改了 `MainLayout.vue` 中的 WebSocket 消息处理逻辑。专门对 `type === 'checkin'`（签到通知）进行了友好解析，并对未知类型的 JSON 消息做了降级容错，避免像图片中那样直接把带有 `{"type":"checkin"...}` 格式的原始字符串抛给用户。 | 已完成 |
| 2026-04-03 | 补齐清单缺失模块与大屏对接 | 新增“分类管理”(后端 CRUD + 管理端页面)；新增“签到管理”(后台签到列表 + 管理员手动签到)；新增“用户管理”(后台用户分页 + 单角色分配，并在注册时默认赋予 volunteer 角色)；Dashboard 接入后端统计的近 7 天报名趋势与分类占比并使用 ECharts 渲染，同时修复前端构建因未使用变量导致的 TS6133。 | 已完成 |
| 2026-04-03 | 超级管理员与管理员权限细分 | 重新定义 RBAC 职责：超级管理员仅负责用户管理与分配“管理员”角色；管理员职责严格限定为“活动管理、报名审核、签到管理”。 | 已完成 |
| 2026-04-03 | 修复超级管理员无法进入管理后台的问题 | 由于角色标识变更，更新了前端 `MainLayout.vue`、`Profile.vue` 和 `Login.vue` 的角色校验逻辑，允许 `super_admin` 访问后台管理入口。 | 已完成 |
| 2026-04-03 | 签到管理模块功能增强 | 在 `AdminCheckinController` 中新增了生成 6 位签到码及 Excel 数据导出功能；前端 `CheckinManage.vue` 增加了生成签到码、数据导出及手动代签的交互界面，提升了现场管理效率。 | 已完成 |
| 2026-04-03 | 优化手动代签体验 | 改进了手动代签的交互逻辑。管理员现在只需输入活动 ID，系统会自动拉取该活动下**已报名但未签到**的用户列表，支持从下拉列表中直接选择用户，无需再手动输入用户 ID。 | 已完成 |
| 2026-04-03 | 修复管理后台大屏图表不显示的问题 | 修复了 `Dashboard.vue` 中 ECharts 初始化时机错误（在 `v-if` 加载状态下初始化导致找不到 DOM 元素）的问题，并补全了缺失的 `router` 引用，整合了生命周期钩子。 | 已完成 |
| 2026-04-03 | 引入 RabbitMQ 异步报名削峰系统 | 为了应对高并发报名场景，引入了 RabbitMQ。将传统的同步报名流程重构为“生产者-消费者”模式：前端请求仅负责入队，后台消费者异步处理配额校验、持久化及结果反馈。这极大地提高了系统的吞吐量和稳定性，并作为项目的技术亮点。 | 已完成 |
| 2026-04-03 | 新增志愿者电子证书功能 | 引入 `iText` 库实现 PDF 动态生成。用户在完成活动签到后，可在活动详情页点击“获取志愿者证书”按钮，一键下载包含姓名、活动名称、志愿时长等信息的正式电子证书。这作为项目的核心亮点，提升了系统的完整性与用户获得感。 | 已完成 |
